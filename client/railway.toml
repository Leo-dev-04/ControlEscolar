[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
watchPatterns = ["client/src/**", "client/*.json", "client/*.html", "client/*.toml"]

[deploy]
startCommand = "npx serve -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"
numReplicas = 1
