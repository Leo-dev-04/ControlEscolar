[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
watchPatterns = ["src/**", "*.json", "*.html"]

[deploy]
startCommand = "npx serve -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"
numReplicas = 1
